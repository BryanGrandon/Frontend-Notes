/** do {
        statement
    }

    while (condition);

 * - Is the same as the while statement except that the condition 
        is evaluated after the statement is executed.
 */

let i = false;

do {
  console.log(i);
} while (i);
