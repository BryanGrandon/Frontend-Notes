/** for (variable of iterable){
        statement
    }
 
 * Executes a loop that operates on a sequence of values 
    coming from an iterable object.
 */

const array = ["a", "b", "c"];

for (const element of array) {
  console.log(element);
}
