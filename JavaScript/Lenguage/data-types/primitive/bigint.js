// The number is converted to a bigint by adding the letter n to the end of the number.
let bigInt = 1n;

// Max Safe Integer

let maxNumber = Number.MAX_SAFE_INTEGER;
console.log(maxNumber);

// BigInt()

BigInt(21);

// not
BigInt(20.4);
BigInt(Infinity);
BigInt(NaN);
